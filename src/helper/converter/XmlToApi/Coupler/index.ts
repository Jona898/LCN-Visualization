import { API, XmlModel } from "@/models";

export function ConvertCouplerFromXmlToApi(
  couplerXml: XmlModel.CouplerList
): API.Coupler {
  if (couplerXml) {
    console.error(
      "Coupler is not Empty Implement Function 'ConvertCouplerFromXmlToApi'!"
    );
  }

  return {};
}
