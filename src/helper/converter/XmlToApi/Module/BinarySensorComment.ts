import { API, XmlModel } from "@/models";

export function ConvertBinarySensorCommentFromXmlToApi(
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  binarySensorCommentXml: XmlModel.BinarySensorCommentList
): API.BinarySensorComment {
  if (binarySensorCommentXml) {
    console.error(
      "Coupler is not Empty Implement Function 'ConvertBinarySensorCommentFromXmlToApi'!"
    );
  }

  return {};
}
